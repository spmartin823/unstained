{
  "name": "@fern-api/tournament",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "compile": "tsc --noEmit",
    "preflight": "tsx src/preflight.ts config.json",
    "start": "tsx src/orchestrator.ts config.json"
  },
  "dependencies": {
    "minimatch": "catalog:",
    "simple-git": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
