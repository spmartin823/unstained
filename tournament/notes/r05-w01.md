# R05-W01 Notes

## State at start of round

- Worktree had `pnpm install` completed (Node 24 path needed for tooling).
- `stainless-equivalency-eval` submodule still uninitialized (gitlink only).
- No prod CLI built at `packages/cli/cli/dist/prod/cli.cjs`.

## Strategy

Every prior round (r01–r04) ended with the winner at `t1=0, t2=0` (eval translation gaps + missing prod CLI → all-null metrics). Tier 3 (earliest scored commit) is decisive.

Plan: commit early to get a scored SHA on the board first, then attempt submodule init + prod build in parallel for a chance at non-null metrics if time permits.
