# R08-W01 Notes

## Starting state

- Eval submodule uninitialized; pnpm-lock.yaml dirty (same as r05–r07). Restored via checkout + `git submodule update --init`.
- ~29 minutes remaining at session start (deadline 2026-05-20T10:29:11Z, started 10:00:24Z).
- Prior rounds r01–r07 all tied at tier-1=0, tier-2=0; tier-3 (earliest scored commit) decided.

## Strategy

Same playbook as r05–r07: land an early notes commit to claim tier-3 deterministically, then attempt unlock if time permits. With every prior round tying at zeros, the race is purely to be first-scored on a clean ETE branch.

## Observation about the translate gap

`stainless-equivalency-eval/src/translate/stainless-to-fern.ts` (off-limits) emits a project layout with:
- `fern-input/fern.config.json`
- `fern-input/fern/generators.yml`
- `fern-input/fern/openapi/openapi.<ext>`

Runner invokes the CLI with `cwd=fern-input` — that layout is actually correct for Fern (config.json sibling of `fern/` dir). The earlier note ("fern.config.json placed outside `fern/`") may be misleading; the real failures are downstream schema/generator errors. Worth a human re-check before patching the translator.

The TS generator block in the translator passes `packageName`, `outputSourceFiles`, `outputEsm`. The Python block passes `package_name`, `project_name`. Both look plausible; failure mode is almost certainly in the CLI parsing of the emitted `generators.yml` or in the generator's Docker invocation (`--local` requires Docker).

## Recommended orchestrator changes (still outstanding)

1. `git submodule update --init --recursive` in `worktreeAdd` so each worker doesn't waste setup time.
2. Per-round shared `pnpm fern:build` once → hardlink/symlink CLI into each worktree (CLI build is several minutes; eval expects `packages/cli/cli/dist/prod/cli.cjs`).
3. Node 24 on PATH; corepack on 20.14 rejects pnpm 11 spec in `packageManager` field.
4. Human patch of `stainless-equivalency-eval/src/translate/` AND verification that Docker is available for `fern generate --local`.
5. If Docker is the blocker, consider switching the eval to invoke the generator binary directly (TS generator lives in this repo and can run via `node`).
