# R06-W01 Notes

## Starting state

- Worktree fresh; eval submodule uninitialized (gitlink only).
- `pnpm-lock.yaml` had `stainless-equivalency-eval` package section deleted (artifact of submodule absence). Restored via `git checkout pnpm-lock.yaml`.
- Submodule initialized via `git submodule update --init stainless-equivalency-eval`.
- Node 24 must be on PATH for husky `pnpm pre-commit` hook (corepack on Node 20.14 fails).
- Prior rounds (r01–r05) all ended with winner at `t1=0, t2=0`; Tier-3 (earliest scored commit) decided.

## Strategy for this round

Same as r05: get a scored SHA on the board as early as possible for tier-3 tiebreak, then attempt non-null metrics if time permits. With only ~25 minutes at session start, no time for `pnpm fern:build` (3+ min) plus iteration.

## Known blockers (carry-forward from r03-w02)

- Eval translate produces Fern configs the CLI cannot parse for some fixtures:
  - `fern.config.json` placed adjacent to `fern/` instead of inside.
  - `lumaai-typescript`: 7 schema errors (e.g., `Type ReframeImageRequest is not defined`, duplicate `GenerationRequest`).
  - `honcho-typescript`: `Unrecognized key(s) in object: 'packageName'` on generators.yml.
- These live under `stainless-equivalency-eval/src/translate/` which is off-limits to workers.
- Path allowlist limits worker edits to `generators/{typescript,python}/**`, `packages/ir-sdk/**`, `packages/generator-migrations/src/generators/{typescript,python}/**`, `pnpm-lock.yaml`, `tournament/notes/**`.

## Recommended orchestrator changes (still outstanding)

1. Run `git submodule update --init --recursive` in `worktreeAdd`.
2. Per-round shared `pnpm fern:build` once → hardlink `packages/cli/cli/dist/prod/cli.cjs` into each worktree.
3. Ensure Node 24 is on the worker's PATH (corepack on 20.14 rejects pnpm 11).
4. Have a human patch `stainless-equivalency-eval/src/translate/` so that the emitted Fern definitions parse cleanly — until that lands, no worker can move metrics off zero.
