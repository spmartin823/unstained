# R03-W01 Notes

## Starting state

- Deadline 2026-05-20T14:43:04Z; session start ~14:14Z (~29 min budget).
- Working tree clean at branch `tournament/r03/w01`, parent at `7f28a1c9a0a tournament r02 w01`.
- All prior rounds (r01–r12) tied at tier-1=0, tier-2=0 — tier-3 (earliest scored commit) decides.

## Strategy

Follow the r05–r12 recipe: land one allowlisted notes commit and `touch .tournament/scoreme`
immediately to claim tier-3, then spend the rest of the clock looking for any behavioral-unlock
lever that fits in a single 29-minute worker.

## Carry-forward blockers (still orchestrator-side, outside the worker allowlist)

Same as r09–r12 notes; nothing in the worker shell can fix these:

1. `pnpm fern:build` is the long pole and is not shared across workers.
2. `git submodule update --init` is not run by `worktreeAdd` — eval submodule lands unpopulated.
3. Node 20.14 corepack rejects pnpm 11 from `packageManager`; Node 24 must be on PATH for the
   husky `pnpm pre-commit` hook to succeed.
4. Worker shell Docker availability for `fern generate --local` is unconfirmed.

Until one of those lands in `automation/tournament/**`, tier-3 timestamp remains the only lever a
single worker can pull inside its clock.
