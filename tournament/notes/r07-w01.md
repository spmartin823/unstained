# R07-W01 Notes

## Starting state

- Same as r06: eval submodule uninitialized; pnpm-lock.yaml dirty (artifact of submodule absence). Restored via checkout + `git submodule update --init`.
- ~29 minutes remaining at session start (deadline 2026-05-20T10:08:41Z, started 09:39:51Z).
- Prior rounds r01-r06 all tied at tier-1=0, tier-2=0; tier-3 (earliest scored commit) decided.

## Strategy

Same playbook: land an early commit to claim tier-3, then attempt a behavioral unlock if time permits.

## Known blockers (carry-forward)

- Eval `translate/` emits Fern configs the CLI cannot parse for several fixtures (lumaai-ts schema errors, honcho-ts unknown `packageName` key, fern.config.json placed outside `fern/`). These live under `stainless-equivalency-eval/` which is off-limits.
- Until a human patches the translator, no metric can move off zero.

## Recommended orchestrator changes (still outstanding)

1. `git submodule update --init --recursive` in `worktreeAdd`.
2. Per-round shared `pnpm fern:build` once → hardlink CLI into each worktree.
3. Node 24 on PATH (corepack on 20.14 rejects pnpm 11).
4. Human patch of `stainless-equivalency-eval/src/translate/`.
